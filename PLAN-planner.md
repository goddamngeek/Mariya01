# Канбан как планировщик дня

Договорённости 31 августа 2026. Обсуждали долго, потому что первые версии
были сложнее нужного — здесь итог, а не путь к нему.

## Суть

Доска остаётся ровно такой, как сейчас. **Ни одной новой колонки, ничего не
переименовывается и не прячется.** Бот добавляет задаче дату и в одном
случае двигает её между колонками.

Колонка отвечает на вопрос «на какой стадии задача», дата — на вопрос
«когда я ей займусь». Это разные вопросы: задача может быть одновременно
запланированной на сегодня и уже в работе. Поэтому «сегодняшность» — метка,
а не колонка. Ранние версии плана делали её колонкой (ИНБОКС перед
БЭКЛОГОМ) и упирались в то, что карточке после назначения даты некуда
уехать.

## Модель

Карточка — дочерняя заметка доски `КАНБАН // KANBAN`; колонка живёт меткой
`#status`, дерево тут ни при чём (см. `read_kanban_status`).

Добавляем две метки:

- `#due` — дата, продвигаемый атрибут, как `readingStart` у книг;
- `#owner` — имя, как у книг.

Всё остальное — производные:

| Что | Как определяется |
|---|---|
| Инбокс | `#status` = БЭКЛОГ, даты нет |
| План на сегодня | `#due` = сегодня, кроме СДЕЛАНО |
| Просрочено | `#due` < сегодня, кроме СДЕЛАНО |

## Владелец

Проставляется сам при добавлении задачи — добавил ты, значит твоя. Ничего
не спрашиваем.

`/inbox` и `/plan` показывают только свои задачи. `/kanban` по-прежнему
показывает доску целиком: видно, кто чем занят.

**Карточка без владельца — общая, видна обоим.** Так ведут себя все
существующие карточки, поэтому разбирать вручную ничего не нужно. Побочный
эффект: общая задача всплывёт в инбоксе у обоих, кто первый разобрал —
того и решение. Для двоих в одной квартире это скорее удобно.

## Три экрана

### `/inbox` — разбор, по одной задаче

```
«Починить кран»
[ Сегодня ]  [ Завтра ]
[ Потом ]    [ Маше ]
```

Тапнул — сразу следующая. Кончились — «Разобрано».

- Сегодня / Завтра — ставит `#due`, колонку не трогает;
- Потом — переносит в `БУДУЩИЕ ЗАДАЧИ` (единственное движение колонок,
  которое делает бот, и это ровно то, что колонка означает);
- Маше / Остапу — меняет `#owner`, задача уходит в инбокс другому.

Календаря в кнопках Telegram нет, поэтому дней ровно два. Понадобится
больше — добавим, это одна строка.

### `/plan` — сегодняшний список

```
На сегодня:
1. Починить кран
2. Позвонить в банк

Просрочено:
3. Оплатить хостинг (28 августа)

[ ✓ Починить кран ]
[ ✓ Позвонить в банк ]
[ ✓ Оплатить хостинг ]
```

«Готово» ставит `#status` = СДЕЛАНО. Дату оставляем — потом видно, что и
когда было сделано.

### Вечером, в чек-ине 21:30

```
Осталось на сегодня:
«Починить кран»
[ На завтра ]  [ Потом ]
```

## Чего сознательно не делаем

- **Кнопки «удалить» нет.** «Потом» закрывает почти все случаи, а настоящий
  мусор удаляется в Trilium за две секунды. Право необратимо стирать
  заметки боту не даём.
- Повторяющихся задач нет.
- Приоритетов нет.
- Лимита задач на день нет.

## Что нужно в коде

- `app/trilium_client.py` — прочитать инбокс, прочитать план на дату,
  проставить `#due`, проставить `#owner`, сдвинуть колонку;
- `app/service.py` — `/inbox` (по одной, с состоянием), `/plan`;
- `app/callbacks.py` — маршруты кнопок;
- `app/main.py`, `app/telegram.py` — команды и меню;
- `app/scheduler.py` — показ остатков в вечернем чек-ине;
- `send_message_with_buttons` сейчас кладёт по кнопке в ряд — нужны ряды
  по две.
