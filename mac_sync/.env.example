BOT_URL=http://localhost:8000
SYNC_BEARER_TOKEN=

ODYSSEUS_URL=https://s-macbook-air-2.tail61af4a.ts.net
ODYSSEUS_TOKEN=

# API key for whichever provider is currently enabled in Odysseus's own
# Admin -> Model Endpoints (Mistral, Yandex, ...). GET /api/models can't
# expose it for security reasons, so it has to be set here by hand whenever
# the provider changes. Leave empty if the endpoint's own configured key
# in Odysseus is sufficient.
LLM_API_KEY=
