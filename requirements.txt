fastapi==0.139.2
uvicorn[standard]==0.51.0
asyncpg==0.31.0
httpx==0.28.1
apscheduler==3.11.3
# Только ради разбора и печати проводок (app/ledger.py) — считаем мы по своей
# же таблице обычным SQL, поэтому beanquery не нужен. Колёса manylinux под
# cp312 есть, компилятор в образ тащить не придётся.
beancount==3.2.3
